# Task ID: 4
# Title: Design Database Schema
# Status: in progress
# Dependencies: 2
# Priority: high

# Description:
Define and implement Django models for all core entities, referencing the Transition State Model and PRD. Include detailed state definitions, metadata, allowed transitions, and validation rules for each workflow and sub-process. Ensure the schema is extensible for future integration and analytics.

# Details:
Create Django models for:
- User, Department, Role, Permission, Group, Session, Token (authentication and access control)
- WorkflowDefinition, State, Transition, WorkflowInstance, StateLog (workflow engine)

# Architectural Note:
# During iterative development, workflow engine models were initially implemented in the users app as part of Task 4. As the architecture matured, these models were migrated to the dedicated workflow_engine app to improve modularity and maintainability. The original models in users were removed to avoid duplication and system check errors. All workflow logic now resides in the workflow_engine app, and this change is reflected in project sequencing and dependencies.
- CreditApplication, Counterparty, LimitRequest, DocumentType, Document (domain models)
- CreditRequestForm, CreditReviewForm, BusinessSponsorshipForm, LegalReviewForm, CreditQuestionnaireForm, CreditAnalysisForm (sub-process/form models)

Use generic relations (GenericForeignKey) for flexible attachments (e.g., Document, WorkflowInstance).
Ensure each state and transition has metadata fields for allowed actions, roles, validation rules, and UI hints.
Design for extensibility (future integrations, analytics, notifications).

# Test Strategy:
- Run migrations and verify all tables are created. (complete)
- Test CRUD operations via Django shell and admin. (outstanding)
- Validate that state definitions are comprehensive and transitions are enforced. (complete)
- Attach documents to various entities and sub-processes and verify via admin/shell. (complete)

# Subtasks:

## 1. User and Authentication Models [complete]
### Task ID: 4.1
### Status: complete
### Dependencies: None
### Priority: high
### Description: Design and implement user and authentication related models
### Details:
- User Model (custom, extends AbstractUser): employee_id, phone_number, role-based permissions, profile info — **Complete**
- Role Model: role hierarchy, permission mapping, role-based access control — **Complete**
- Department Model: user grouping/organization — **Complete**
- Permission Model: resource/action permissions — **Complete**
- Group Model: user group management, group permissions, group membership (extend Django’s built-in if needed) — *Not yet explicitly extended or customized (using Django built-in)*
- Session Model: session tracking, security, management (extend if needed) — *Not yet explicitly extended or customized (using Django built-in)*
- Token Model: for API/auth (if required) — *Not yet implemented*

**Note:**
- All required custom models (User, Role, Department, Permission) are implemented, migrated, and admin-registered.
- Group and Session currently use Django’s built-in models; extension/customization can be addressed if needed.
- Token model for API/auth is not yet implemented and will be added if required by future tasks.

## 2. Workflow Engine Models [complete]
### Task ID: 4.2
### Status: complete
### Dependencies: 4.1
### Priority: high
### Description: Implement core workflow engine models
### Details:
- WorkflowDefinition, State (with metadata), Transition (with metadata), WorkflowInstance (generic), StateLog
- Each sub-process/form has its own workflow instance
- States and transitions must include metadata for allowed actions, roles, validation, and UI hints

## 3. Domain Models [complete]
### Task ID: 4.3
### Status: complete
### Dependencies: 4.2
### Priority: high
### Description: Implement credit application and related domain models
### Details:
- CreditApplication, Counterparty, LimitRequest, DocumentType, Document
- Document uses GenericForeignKey for flexible attachment to any entity or sub-process

## 4. Sub-Process/Form Models [complete]
### Task ID: 4.4
### Status: complete
### Dependencies: 4.3
### Priority: high
### Description: Implement models for each workflow sub-process/form
### Details:
- CreditRequestForm, CreditReviewForm, BusinessSponsorshipForm, LegalReviewForm, CreditQuestionnaireForm, CreditAnalysisForm, CreditCompilationForm, CreditApprovalForm
- Each linked to parent application and with its own workflow instance
- Ensure all forms/sub-processes described in PRD (v3).md are implemented. Cross-check PRD for any additional forms required by future workflow phases.

## 5. Testing and Validation [in progress]
### Task ID: 4.5
### Status: in progress
### Dependencies: 4.1, 4.2, 4.3, 4.4
### Priority: high
### Description: Test and validate schema implementation
### Details:
- Run migrations, verify tables (complete)
- Test CRUD (outstanding)
- Validate state/transition logic (complete)
- Document attachment (complete)

# Outstanding Work:
- Write and execute tests for CRUD operations on all models and workflows.
- Further develop and test audit trail and event logging (StateLog, transition logs, user actions).

# References:
- Transition State Model (requirements folder)
- PRD (v3).md (requirements folder)
- Entity Relationship Diagram (requirements folder)
   - Session security
   - Session management
5. Token Model:
   - JWT token management
   - Token refresh
   - Token validation
6. Audit Trail:
   - User activity logging
   - Permission changes
   - Login/logout tracking

## 2. Credit Request Models [complete]
### Dependencies: 4.2
### Description: Design and implement models for credit requests and related entities
### Details:
1. CreditRequest Model:
   - Request number (auto-generated)
   - Request status
   - Request type
   - Counterparty information
   - Limit details
   - Revenue information
   - Document references
2. Limit Model:
   - Limit types (Trading, Nostro, Loan)
   - Limit amounts
   - Currency
   - Maturity dates
   - Conditions
3. Counterparty Model:
   - Basic information
   - Credit ratings
   - Risk classification
   - Relationship details
4. Document Model:
   - Document types
   - File storage
   - Version control
   - Access permissions
5. Audit Trail:
   - Request history
   - Status changes
   - User actions
6. Workflow State:
   - Current state
   - Previous states
   - Transition logs

## 3. Workflow Engine Models [complete]
### Dependencies: 4.2
### Description: Design and implement models for workflow engine and state management
### Details:
1. Workflow Model:
   - Workflow definition
   - State transitions
   - Business rules
   - Event triggers
2. State Model:
   - State types
   - State transitions
   - State validation
   - State metadata
   - Detailed state definitions (name, description, metadata, allowed transitions, validation rules)
3. Transition Model:
   - Transition rules
   - Transition conditions
   - Transition actions
   - Transition logs
   - Explicit reference to Transition State Model for managing state changes
4. Event Model:
   - Event types
   - Event triggers
   - Event handlers
   - Event logs
5. Business Rule Model:
   - Rule definitions
   - Rule conditions
   - Rule actions
   - Rule validation
6. Audit Trail:
   - Workflow history
   - State changes
   - Transition logs
   - Rule execution

## 4. Define Detailed State Definitions and Transition State Model References [pending]
### Dependencies: 4.3
### Description: Expand on state definitions and ensure all workflow and credit request models reference the Transition State Model.
### Details:
1. For each state in the workflow, define:
   - Name
   - Description
   - Metadata (e.g., timestamps, responsible roles)
   - Allowed transitions (with references to Transition State Model)
   - Validation rules for entering/exiting the state
2. Update all relevant models (Workflow, WorkflowInstance, CreditRequest, etc.) to reference the Transition State Model for state management.
3. Document the state lifecycle and transition logic in the schema.

